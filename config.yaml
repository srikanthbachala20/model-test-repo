model:
  id: "model-id"
  user_id: "user-id"
  app_id: "app-id"
  model_type_id: "text-to-text"

build_info:
  python_version: "3.12"

inference_compute_info:
  cpu_limit: "1"
  cpu_memory: 500Mi
  num_accelerators: 0
